<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/alarm_item_height"
    android:layout_marginBottom="@dimen/item_list_vertical_padding"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/alarm_clock_icon"
        android:layout_width="@dimen/icon_item_size"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@drawable/clock_alarm_icon"
        android:contentDescription="Alarm clock icon" />


    <TextView
        android:id="@+id/item_clock_alarm_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/alarm_clock_icon"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="Alarm"
        />

</androidx.constraintlayout.widget.ConstraintLayout>